spring:
  data:
    mongodb:
      uri: mongodb://rootuser:rootpassword@localhost:27017/library?authSource=admin
      # authSource=admin указывает, что аутентификация должна происходить в базе данных admin
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
    org.mongodb.driver: INFO